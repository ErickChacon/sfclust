% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-clusters.R
\name{raster_adjacency}
\alias{raster_adjacency}
\title{Build a 4-connected (Rook) adjacency matrix for a regular grid}
\usage{
raster_adjacency(nx, ny)
}
\arguments{
\item{nx}{Integer. Number of cells in the x direction (columns).}

\item{ny}{Integer. Number of cells in the y direction (rows).}
}
\value{
A sparse symmetric \code{Matrix} of size \verb{(nx * ny) x (nx * ny)} with 1s at
adjacent cell pairs.
}
\description{
Constructs the sparse symmetric adjacency matrix for an \code{nx} by \code{ny} regular grid
under 4-connectivity (each cell connected to its left, right, top, and bottom
neighbors). The flat cell index is \code{(iy - 1) * nx + ix}.
}
\examples{
library(sfclust)
A <- raster_adjacency(3, 2)
print(A)

}
