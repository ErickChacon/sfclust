% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sfclust-package.R
\docType{data}
\name{simu_data}
\alias{simu_data}
\title{Simulated Spatiotemporal and Spatial Data}
\format{
A list with two elements:
\describe{
\item{\code{stdata}}{A \code{stars} object containing spatiotemporal observations.}
\item{\code{geodata}}{A \code{sf} object containing Voronoi polygons with cluster memberships.}
}
}
\description{
A list containing two objects for demonstrating Bayesian spatial functional clustering:
}
\details{
\itemize{
\item \code{stdata} contains simulated spatiotemporal data where rows are time points and columns represent spatial units.
\item \code{geodata} includes spatial Voronoi polygons and their associated metadata, such as cluster memberships.
}
}
\examples{
data(simu_data)

# Access spatiotemporal data
stdata <- simu_data$stdata
plot(stdata)

# Access spatial polygons
geodata <- simu_data$geodata
library(ggplot2)
ggplot(geodata) +
  geom_sf(aes(fill = factor(membership))) +
  labs(title = "Spatial Clusters", fill = "Cluster") +
  theme_minimal()

}
\keyword{datasets}
