% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-within.R
\name{sfclust_within_model}
\alias{sfclust_within_model}
\title{Create a within-cluster model specification}
\usage{
sfclust_within_model(
  formula = NULL,
  family,
  stack_fun = NULL,
  fitted_fun = NULL
)
}
\arguments{
\item{formula}{An INLA formula. It may be \code{NULL} only when \code{stack_fun}
returns a cluster-specific formula.}

\item{family}{The INLA family specification.}

\item{stack_fun}{Optional function called once per cluster. It receives the
long-format cluster data and returns either an \code{INLA::inla.stack()} object
or a list with components \code{stack} and optional \code{formula}.}

\item{fitted_fun}{Optional function used by \code{fitted()} to extract fitted
values from custom within-cluster models.}
}
\value{
An object of class \code{"sfclust_within_model"}.
}
\description{
Defines the INLA model fitted inside each cluster. The usual formula and
family interface remains available directly in \code{sfclust()}. This object is
useful when the within-cluster model needs a data preparation step,
such as an INLA stack or a custom function to obtain fitted values.
}
\details{
\code{stack_fun} is called as \code{stack_fun(data)}, where \code{data} contains the rows
for one cluster. The data are in the same long format used internally by
\code{sfclust()}: \code{id} is the row position in the original data, \code{ids} is the
spatial unit id, \code{sid} is the valid spatial unit id used by the clustering
algorithm, and columns such as \code{id_time} identify functional dimensions.

\code{stack_fun(data)} may return an INLA stack directly:

\preformatted{
INLA::inla.stack(...)
}

or a list:

\preformatted{
list(
  stack = INLA::inla.stack(...),
  formula = optional_formula
)
}

The \code{formula} component is optional. Family and other INLA arguments should
be provided through \code{sfclust_within_model()} or \code{sfclust()}, not returned by
\code{stack_fun}.
}
\examples{
model <- sfclust_within_model(
  formula = y ~ 1,
  family = "gaussian"
)

}
